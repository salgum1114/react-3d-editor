import { IComponentDescriptor } from '../components';

const file: IComponentDescriptor = {
    isSingleProp: true,
    name: 'file',
    schema: {
        type: 'file',
    },
}

export default file;
