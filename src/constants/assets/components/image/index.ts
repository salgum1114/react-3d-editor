import { IComponentDescriptor } from '../components';

const image: IComponentDescriptor = {
    isSingleProp: true,
    name: 'image',
    schema: {
        type: 'image',
    },
};

export default image;
