import { IComponentDescriptor } from '../components';

const video: IComponentDescriptor = {
    isSingleProp: true,
    name: 'video',
    schema: {
        type: 'video',
    },
};

export default video;
