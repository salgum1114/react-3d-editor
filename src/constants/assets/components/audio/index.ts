import { IComponentDescriptor } from '../components';

const audio: IComponentDescriptor = {
    isSingleProp: true,
    name: 'audio',
    schema: {
        type: 'audio',
    },
};

export default audio;
