import { IComponentDescriptor } from '../components';

const name: IComponentDescriptor = {
    isSingleProp: true,
    name: 'name',
    schema: {
        type: 'string',
    },
}

export default name;
