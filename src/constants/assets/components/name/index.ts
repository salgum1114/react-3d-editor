import { IComponentDescriptor } from '../../../../models/component';

const name: IComponentDescriptor = {
	isSingleProp: true,
	name: 'name',
	schema: {
		type: 'string',
	},
};

export default name;
